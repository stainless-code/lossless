---
name: typescript-best-practices
description: TypeScript rules for this repo. Use when editing or reviewing any .ts file, when adding an `as` cast, when a type needs a comment to explain which field combinations are valid, or when mapping SQLite rows, Pi hook payloads, or config JSON into domain types.
---

# TypeScript best practices

Apply the type-system-discipline section of [`principles/REFERENCE.md`](../principles/REFERENCE.md#type-system-discipline) first. This file grounds it in syntax.

| Rule                             | Summary                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Discriminated unions             | Model variants with a `kind` literal discriminant so impossible states can't be represented. No optional-field bags.                                                                                                                                                                                                                                                                                           |
| Branded types                    | Brand primitives with `& { readonly __brand: "X" }` so they can't be mixed up. Validate once at the boundary.                                                                                                                                                                                                                                                                                                  |
| Constructive modeling            | Build the shape so the illegal value can't be constructed. `[T, ...T[]]` for non-empty, `start` plus `duration` for a range. Not a runtime guard.                                                                                                                                                                                                                                                              |
| Simplest total type              | Keep `T[]` while every operation on it stays total. Strengthen to `NonEmpty<T>` only where the loose type forces `!`, a cast, or a "should never happen" throw.                                                                                                                                                                                                                                                |
| `unknown` over `any`             | External data is `unknown`.                                                                                                                                                                                                                                                                                                                                                                                    |
| Schemas before guards            | Before hand-writing a property-by-property type guard, use the repository's runtime schema library and infer the type from the schema. This repo has `typebox` as a peer (tool parameters in `tools/recall.ts`); `config-io.ts` owns the config file boundary.                                                                                                                                                 |
| No `as` casts                    | Every `as` is a runtime crash waiting. Cast only after validation. A cast between structurally incompatible types does not error there: it collapses handler-return inference and reports at the enclosing `pi.on` call, so the named site sits far from the cause.                                                                                                                                            |
| Narrowing hierarchy              | Discriminant switch > `in` operator > `typeof`/`instanceof` > user-defined type guard > `as`.                                                                                                                                                                                                                                                                                                                  |
| Type guards                      | Must verify the claim. A lying guard is worse than `as`. Name them `isX` or `hasX`.                                                                                                                                                                                                                                                                                                                            |
| Exhaustiveness                   | Inline `const _exhaustive: never = x;` in default arms so the compiler errors when a new variant is added.                                                                                                                                                                                                                                                                                                     |
| `satisfies` over `as`            | Validates the value without widening literal types.                                                                                                                                                                                                                                                                                                                                                            |
| Boundary validation              | Parse where data crosses in, into a named domain type. In this repo the boundaries are Pi hook payloads (`index.ts`), `node:sqlite` rows (`store.ts`), the config file (`config-io.ts`), and the LLM response text (`summarize.ts`). `Record<string, unknown>` stops at that parse. Trust types inside.                                                                                                        |
| Schema-derived types             | Reach for `Pick`/`Omit`/`Parameters`/`ReturnType`/`Awaited`/`typeof` before declaring a new interface.                                                                                                                                                                                                                                                                                                         |
| Object args                      | Pass objects, not positional, so argument order is self-documenting. Skip on hot paths.                                                                                                                                                                                                                                                                                                                        |
| Real tests                       | Don't mock what you can run. The store runs in-memory (`:memory:`), so store and compaction tests use the real store, not a fake. Mock only the LLM. A policy test that injects a store-shaped dep (`frontier`, a span lookup) is still a store fake: keep it for geometry, and pair it with at least one test that binds the real store's method so a fake returning what the author hoped cannot pass alone. |
| No `console.log` in shipped code | Diagnostics go through the Pi `ctx.ui` surface or the metrics log.                                                                                                                                                                                                                                                                                                                                             |

Examples: [`PATTERNS.md`](./PATTERNS.md).
